# Console

The admin UI is documented as the **[Ylang Portal](portal.md)** (`/console/*`).

That guide covers login, every hub (Today / Quality / Routing / Control),
library pages, Parameters, screenshots, and JSON APIs.

Legacy URL `/console` in the browser still maps to Overview; this page exists
so older links into `docs/console.md` keep working.
